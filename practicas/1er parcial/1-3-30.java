//REVERSED LINKED LIST
// Escribir una función que tomando el primer nodo de una linkedlist como argumento,
//revierta todo el orden de los elementos
//DEBE SER DESTRUCTIVO - CORREGIR!!

"""public void func(Node<E> p){
    Node<E> temp = new Node();
    temp.item = p.item;
    temp.next = null;

    while(p.next != null){
        p = p.next;
        Node<E> temp2 = newNode();
        temp2.item = p.item;
        temp2.next = temp;
        temp = temp2;
    }

    return temp2;
}"""

//VERSION CORREGIDA - DESTRUCTIVA
//orden n --> no me queda otra que recorrer una lista de n elementos
public class secondExercise{
    public static void main(String[] args) {
    }
    public Node reverseLinkedList(Node<E> firstnode){
        Node<E> currentNode = first;
        Node<E> previousNode;
        Node<E> nextNode;
    }
    return previousNode;

}
